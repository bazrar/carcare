import { CreateProviderServiceDto } from './create-provider-service.dto';

describe('CreateProviderServiceDto', () => {
  it('should be defined', () => {
    expect(new CreateProviderServiceDto()).toBeDefined();
  });
});
