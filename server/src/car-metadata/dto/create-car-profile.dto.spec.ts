import { CreateCarProfileDto } from './create-car-profile.dto';

describe('CreateCarProfileDto', () => {
  it('should be defined', () => {
    expect(new CreateCarProfileDto()).toBeDefined();
  });
});
