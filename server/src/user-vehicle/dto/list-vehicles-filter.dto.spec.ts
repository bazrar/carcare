import { ListVehiclesFilterDto } from './list-vehicles-filter.dto';

describe('ListVehiclesFilterDto', () => {
  it('should be defined', () => {
    expect(new ListVehiclesFilterDto()).toBeDefined();
  });
});
