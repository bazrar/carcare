import { FilterServiceStationListDto } from './list-service-station-filter.dto';

describe('FilterServiceStationListDto', () => {
  it('should be defined', () => {
    expect(new FilterServiceStationListDto()).toBeDefined();
  });
});
