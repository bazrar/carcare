import { SearchServiceStationDto } from './search-service-station.dto';

describe('SearchServiceStationDto', () => {
  it('should be defined', () => {
    expect(new SearchServiceStationDto()).toBeDefined();
  });
});
