import { UpdateServiceStationDto } from './update-service-station.dto';

describe('UpdateServiceStationDto', () => {
  it('should be defined', () => {
    expect(new UpdateServiceStationDto()).toBeDefined();
  });
});
